# context/

Drop **`default_600k.md`** here — Neel's compiled 600k-token mech-interp
context file (docs, key library source, ARENA tutorials, key papers, his
blog posts).

It is gitignored: 637 KB, and it is his compilation rather than ours to
redistribute. Source: the Drive folder linked from
`llm/application/mats12-instructions-raw.txt` (search for "600k token file").

Loading it is ADV-11 in the conformance register. Neel: *"Context is
crucial... By default, just put this 600k token file in the context window."*
