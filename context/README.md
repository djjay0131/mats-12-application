# context/

**`default_600k.md`** — Neel's compiled mech-interp context file: his research
process posts, the mech interp explainer and glossary, key papers, ARENA
tutorials, and source for TransformerLens and nnsight. 2.24 MB, 41,360 lines.

Load it into your context window before doing research work. This is ADV-11 in
the conformance register. Neel: *"Context is crucial... By default, just put
this 600k token file in the context window."*

It is committed rather than fetched, so a fresh clone on ARC has everything it
needs in one step.

⚠️ It is his compilation, kept here because this repository is **private**.
Do not make this repository public without removing the file first.
